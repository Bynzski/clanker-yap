{
  "name": "clanker-yap",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "tauri:build": "NO_STRIP=1 tauri build --bundles appimage",
    "tauri:dev": "npx tauri dev",
    "verify:rust": "cd src-tauri && cargo test && cargo clippy -- -D warnings && cargo fmt --check",
    "release:verify": "npm run verify:rust && npm run tauri:build",
    "hooks:install": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.10.1"
  }
}
